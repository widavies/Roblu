package com.cpjd.roblu.ui.forms;

import com.cpjd.roblu.forms.elements.Element;

interface EditListener {
    void postEdit(Element e); // means we need to launch and add element to handle this edit request
}
